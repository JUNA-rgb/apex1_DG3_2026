:root{
    --primary:#39a0da;
    --secondary:#0f1425;
    --dark:#050505;
    --light:#ffffff;
    --gray:#a0a0a0;
}

*{
    margin:0;
    padding:0;
    box-sizing:border-box;
}

html{
    scroll-behavior:smooth;
}

body{
    font-family:'Inter',sans-serif;
    background:#050505;
    color:white;
    overflow-x:hidden;
}

/* NAVBAR */

header{
    position:fixed;
    width:100%;
    top:0;
    z-index:999;
    backdrop-filter:blur(15px);
    background:rgba(5,5,5,.75);
    border-bottom:1px solid rgba(57,160,218,.2);
}

.nav{
    max-width:1400px;
    margin:auto;
    padding:20px 40px;
    display:flex;
    justify-content:space-between;
    align-items:center;
}

.logo{
    font-size:1.8rem;
    font-weight:900;
    color:var(--primary);
    letter-spacing:2px;
}

.menu{
    display:flex;
    gap:40px;
}

.menu a{
    text-decoration:none;
    color:white;
    transition:.3s;
}

.menu a:hover{
    color:var(--primary);
}

/* HERO */

.hero{
    min-height:100vh;
    display:flex;
    align-items:center;
    padding:0 10%;
    position:relative;
}

.hero::before{
    content:"";
    position:absolute;
    inset:0;
    background:
    radial-gradient(circle at center,
    rgba(57,160,218,.25),
    transparent 60%);
}

.hero-content{
    max-width:700px;
    z-index:2;
}

.tag{
    color:var(--primary);
    font-weight:700;
    margin-bottom:20px;
}

.hero h1{
    font-size:5rem;
    line-height:1;
    margin-bottom:30px;
}

.hero p{
    color:#bdbdbd;
    font-size:1.2rem;
    line-height:1.8;
}

.cta{
    margin-top:40px;
    display:flex;
    gap:20px;
}

.btn{
    padding:18px 40px;
    border:none;
    cursor:pointer;
    font-weight:700;
    transition:.3s;
}

.btn-primary{
    background:var(--primary);
    color:white;
    box-shadow:0 0 30px rgba(57,160,218,.5);
}

.btn-primary:hover{
    transform:translateY(-4px);
}

.btn-secondary{
    background:transparent;
    border:1px solid var(--primary);
    color:white;
}

/* CARDS */

.section{
    padding:120px 10%;
}

.section-title{
    font-size:3rem;
    margin-bottom:60px;
}

.grid{
    display:grid;
    grid-template-columns:repeat(auto-fit,minmax(320px,1fr));
    gap:30px;
}

.card{
    background:#0f1425;
    border:1px solid rgba(57,160,218,.15);
    padding:35px;
    transition:.4s;
    border-radius:20px;
}

.card:hover{
    transform:translateY(-10px);
    border-color:var(--primary);
    box-shadow:0 0 40px rgba(57,160,218,.15);
}

.card h3{
    margin-bottom:20px;
}

/* PRIZES */

.prize-box{
    text-align:center;
    padding:60px;
    background:linear-gradient(
    135deg,
    #0f1425,
    #151d35
    );
    border-radius:25px;
}

.prize-box h2{
    font-size:5rem;
    color:var(--primary);
}

/* FOOTER */

footer{
    padding:80px 10%;
    border-top:1px solid rgba(255,255,255,.1);
    text-align:center;
}

footer p{
    color:#777;
}