import React, { useEffect, useRef, useState } from 'react';
import { ChevronDown, Trophy, Users, Flag, DollarSign, Monitor, Headphones, Zap, Shield, Star, Target, ArrowRight, Quote, Check, Plus, Minus } from 'lucide-react';

/* ───────── images ───────── */
const HERO_BG    = "https://media.base44.com/images/public/6a399908123577d00590f28d/b9127bb01_generated_1c1d20f6.png";
const SIM_EXP    = "https://media.base44.com/images/public/6a399908123577d00590f28d/fec0d3903_generated_eef2308e.png";
const PODIUM_IMG = "https://media.base44.com/images/public/6a399908123577d00590f28d/d6006f557_generated_66077a5f.png";
const COURSES_IMG = "https://media.base44.com/images/public/6a399908123577d00590f28d/01aef9ffb_generated_e5f90e41.png";
const FACILITY   = "https://media.base44.com/images/public/6a399908123577d00590f28d/a5a0a3e20_generated_53a22a27.png";
const TELEMETRY  = "https://media.base44.com/images/public/6a399908123577d00590f28d/1a0ad700b_generated_b263b01c.png";

/* ───────── hooks ───────── */
function useInView(threshold = 0.15) {
  const ref = useRef(null);
  const [visible, setVisible] = useState(false);
  useEffect(() => {
    const el = ref.current;
    if (!el) return;
    const obs = new IntersectionObserver(
      ([e]) => { if (e.isIntersecting) { setVisible(true); obs.unobserve(el); } },
      { threshold }
    );
    obs.observe(el);
    return () => obs.disconnect();
  }, [threshold]);
  return [ref, visible];
}

/* ───────── reusable atoms ───────── */
function CountUp({ end, suffix = '', prefix = '', duration = 2000 }) {
  const [val, setVal] = useState(0);
  const [ref, visible] = useInView(0.3);
  useEffect(() => {
    if (!visible) return;
    let cur = 0;
    const step = end / (duration / 16);
    const id = setInterval(() => {
      cur += step;
      if (cur >= end) { setVal(end); clearInterval(id); }
      else setVal(Math.floor(cur));
    }, 16);
    return () => clearInterval(id);
  }, [visible, end, duration]);
  return <span ref={ref}>{prefix}{val.toLocaleString()}{suffix}</span>;
}

function Reveal({ children, className = '', delay = 0, direction = 'up' }) {
  const [ref, visible] = useInView(0.1);
  const dirs = { up: 'translate-y-12', down: '-translate-y-12', left: 'translate-x-12', right: '-translate-x-12' };
  return (
    <div
      ref={ref}
      className={`transition-all duration-700 ease-out ${visible ? 'opacity-100 translate-x-0 translate-y-0' : `opacity-0 ${dirs[direction]}`} ${className}`}
      style={{ transitionDelay: `${delay}ms` }}
    >
      {children}
    </div>
  );
}

/* ───────── data ───────── */
const STATS = [
  { icon: Users, value: 500, suffix: '+', prefix: '', label: 'Alumnos activos' },
  { icon: Flag, value: 120, suffix: '+', prefix: '', label: 'Carreras realizadas' },
  { icon: Trophy, value: 50, suffix: '+', prefix: '', label: 'Campeones formados' },
  { icon: DollarSign, value: 50000, suffix: '', prefix: '$', label: 'En premios entregados' },
];

const ROADMAP = [
  { level: 'F3', title: 'Fórmula 3', subtitle: 'Nivel Inicial', desc: 'Aprendé los fundamentos del sim racing profesional. Dominio de frenado, líneas de carrera y gestión de neumáticos.', benefits: ['Simulador básico', 'Coach grupal', 'Liga interna', '3 meses'] },
  { level: 'F2', title: 'Fórmula 2', subtitle: 'Nivel Intermedio', desc: 'Perfeccioná tu técnica con telemetría avanzada, estrategia de carrera y manejo bajo presión.', benefits: ['Simulador avanzado', 'Coach personal', 'Liga competitiva', '6 meses'] },
  { level: 'F1', title: 'Fórmula 1', subtitle: 'Nivel Profesional', desc: 'Competí al máximo nivel. Accedé a campeonatos oficiales con premios en efectivo y reconocimiento global.', benefits: ['Simulador top-tier', 'Coaching élite', 'Campeonato oficial', '12 meses'] },
];

const COURSE_PLANS = [
  { tier: 'F3', name: 'Fórmula 3', tagline: 'Donde empieza todo', price: '$199/mes', color: '#39A0DA', features: ['Acceso a simuladores', 'Clases grupales semanales', 'Liga F3 interna', 'Análisis de telemetría básico', 'Comunidad Discord'] },
  { tier: 'F2', name: 'Fórmula 2', tagline: 'Llevá tu velocidad al límite', price: '$349/mes', color: '#00E5FF', popular: true, features: ['Todo lo de F3', 'Coaching 1 a 1', 'Liga F2 competitiva', 'Telemetría avanzada', 'Acceso a eventos especiales'] },
  { tier: 'F1', name: 'Fórmula 1', tagline: 'Nivel campeonato mundial', price: '$599/mes', color: '#FFFFFF', features: ['Todo lo de F2', 'Coaching élite ilimitado', 'Campeonato oficial APEX', 'Premios en efectivo', 'Programa de patrocinio'] },
];

const STANDINGS = [
  { pos: 1, name: 'Carlos "Lightning" Reyes', team: 'APEX Racing', pts: 387, flag: '🇦🇷' },
  { pos: 2, name: 'Lucas "Storm" Martínez', team: 'Velocity eSports', pts: 356, flag: '🇲🇽' },
  { pos: 3, name: 'Andrés "Apex" Gómez', team: 'APEX Racing', pts: 341, flag: '🇨🇴' },
  { pos: 4, name: 'Martín "Turbo" López', team: 'Speed Academy', pts: 298, flag: '🇨🇱' },
  { pos: 5, name: 'Diego "Nitro" Sánchez', team: 'Velocity eSports', pts: 275, flag: '🇪🇸' },
];

const TESTIMONIALS = [
  { name: 'Carlos Reyes', title: 'Campeón Temporada 4', quote: 'APEX me transformó de un casual gamer a un piloto competitivo. Las herramientas, el coaching y la comunidad son de otro nivel. Gané mi primer campeonato y $5.000 en premios.', avatar: '🏎️' },
  { name: 'Valentina Torres', title: 'Piloto F2', quote: 'Nunca pensé que el sim racing pudiera ser tan profesional. La academia tiene un nivel que no encontrás en ningún otro lugar. Cada carrera se siente como una final de F1.', avatar: '🏁' },
  { name: 'Martín López', title: 'Ex-alumno F3, ahora F1', quote: 'Empecé en F3 sin saber nada. Hoy compito en la categoría máxima y sueño con el campeonato. APEX no es solo una academia, es un estilo de vida.', avatar: '🏆' },
  { name: 'Diego Fernández', title: 'Piloto Profesional', quote: 'Como piloto real, uso APEX para entrenar entre competencias. Los simuladores son increíbles y el nivel de detalle en la telemetría rivaliza con equipos reales.', avatar: '⚡' },
];

const WHY_US = [
  { icon: Monitor, title: 'Simuladores Profesionales', desc: 'Equipos de última generación con motion platforms y force feedback directo.' },
  { icon: Headphones, title: 'Coaching Especializado', desc: 'Instructores con experiencia en motorsport real y sim racing competitivo.' },
  { icon: Trophy, title: 'Competencias Reales', desc: 'Campeonatos oficiales con clasificaciones, rankings y temporadas completas.' },
  { icon: DollarSign, title: 'Premios en Efectivo', desc: 'Ganá dinero real compitiendo. +$50.000 entregados a nuestros campeones.' },
  { icon: Users, title: 'Comunidad Exclusiva', desc: 'Red de pilotos, coaches y fanáticos del automovilismo de toda Latinoamérica.' },
  { icon: Zap, title: 'Tecnología de Punta', desc: 'Telemetría avanzada, análisis de datos y herramientas profesionales.' },
];

const FAQS = [
  { q: '¿Necesito experiencia previa?', a: 'No. Nuestro programa F3 está diseñado para principiantes absolutos. Te enseñamos desde cero con metodología profesional.' },
  { q: '¿Qué simuladores utilizan?', a: 'Usamos iRacing, Assetto Corsa Competizione y rFactor 2, con hardware Fanatec, Heusinkveld y plataformas de movimiento Qubic.' },
  { q: '¿Cómo funcionan los campeonatos?', a: 'Cada temporada dura entre 8 y 12 semanas. Hay clasificaciones semanales, standings en tiempo real, y finales con podio y premios en efectivo.' },
  { q: '¿Puedo competir desde mi casa?', a: 'Sí. Tenemos modalidad online y presencial. Solo necesitás un volante compatible, PC y conexión estable para participar remotamente.' },
  { q: '¿Los premios son reales?', a: 'Absolutamente. Entregamos más de $50.000 por temporada en premios, distribuidos entre las tres categorías (F3, F2 y F1).' },
  { q: '¿Hay límite de edad?', a: 'El programa está pensado para mayores de 16 años. No hay límite superior. Tenemos pilotos desde 16 hasta 55 años compitiendo.' },
];

/* ───────── subcomponents ───────── */
function Navbar() {
  const [scrolled, setScrolled] = useState(false);
  const [open, setOpen] = useState(false);
  useEffect(() => {
    const h = () => setScrolled(window.scrollY > 60);
    window.addEventListener('scroll', h);
    return () => window.removeEventListener('scroll', h);
  }, []);
  const links = [
    { label: 'Inicio', href: '#hero' },
    { label: 'Academia', href: '#about' },
    { label: 'Cursos', href: '#courses' },
    { label: 'Campeonato', href: '#championship' },
    { label: 'Premios', href: '#prizes' },
    { label: 'FAQ', href: '#faq' },
  ];
  return (
    <nav className={`fixed top-0 left-0 right-0 z-50 transition-all duration-500 ${scrolled ? 'bg-[#050505]/95 backdrop-blur-xl border-b border-[#39A0DA]/20 shadow-lg shadow-[#39A0DA]/5' : 'bg-transparent'}`}>
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 flex items-center justify-between h-16 lg:h-20">
        <a href="#hero" className="flex items-center gap-2 group">
          <div className="w-8 h-8 lg:w-10 lg:h-10 rounded bg-gradient-to-br from-[#39A0DA] to-[#00E5FF] flex items-center justify-center font-heading font-black text-[#050505] text-sm lg:text-base group-hover:shadow-lg group-hover:shadow-[#39A0DA]/40 transition-shadow">A</div>
          <span className="font-heading font-bold text-white text-sm lg:text-lg tracking-wider">APEX<span className="text-[#39A0DA]"> SIM-RACING</span></span>
        </a>
        <div className="hidden lg:flex items-center gap-8">
          {links.map(l => (
            <a key={l.href} href={l.href} className="text-sm text-white/70 hover:text-[#00E5FF] transition-colors font-body tracking-wide">{l.label}</a>
          ))}
          <a href="#cta" className="px-5 py-2.5 bg-gradient-to-r from-[#39A0DA] to-[#00E5FF] text-[#050505] font-heading font-bold text-xs tracking-widest rounded hover:shadow-lg hover:shadow-[#39A0DA]/40 transition-all hover:scale-105">INSCRIBIRME</a>
        </div>
        <button onClick={() => setOpen(!open)} className="lg:hidden text-white p-2">
          <div className="w-6 flex flex-col gap-1.5">
            <span className={`block h-0.5 bg-[#39A0DA] transition-all duration-300 ${open ? 'rotate-45 translate-y-2' : ''}`} />
            <span className={`block h-0.5 bg-[#39A0DA] transition-all duration-300 ${open ? 'opacity-0' : ''}`} />
            <span className={`block h-0.5 bg-[#39A0DA] transition-all duration-300 ${open ? '-rotate-45 -translate-y-2' : ''}`} />
          </div>
        </button>
      </div>
      {open && (
        <div className="lg:hidden bg-[#050505]/98 backdrop-blur-xl border-t border-[#39A0DA]/20 px-6 pb-6 pt-4 space-y-4">
          {links.map(l => (
            <a key={l.href} href={l.href} onClick={() => setOpen(false)} className="block text-white/80 hover:text-[#00E5FF] transition-colors font-body py-1">{l.label}</a>
          ))}
          <a href="#cta" onClick={() => setOpen(false)} className="block text-center px-5 py-3 bg-gradient-to-r from-[#39A0DA] to-[#00E5FF] text-[#050505] font-heading font-bold text-xs tracking-widest rounded">INSCRIBIRME</a>
        </div>
      )}
    </nav>
  );
}

function ParticleField() {
  const canvasRef = useRef(null);
  useEffect(() => {
    const c = canvasRef.current;
    if (!c) return;
    const ctx = c.getContext('2d');
    let w = c.width = c.offsetWidth;
    let h = c.height = c.offsetHeight;
    const particles = Array.from({ length: 70 }, () => ({
      x: Math.random() * w, y: Math.random() * h,
      vx: (Math.random() - 0.5) * 0.4, vy: (Math.random() - 0.5) * 0.4,
      r: Math.random() * 2 + 0.5, a: Math.random() * 0.4 + 0.2
    }));
    let raf;
    const draw = () => {
      ctx.clearRect(0, 0, w, h);
      particles.forEach(p => {
        p.x += p.vx; p.y += p.vy;
        if (p.x < 0) p.x = w; if (p.x > w) p.x = 0;
        if (p.y < 0) p.y = h; if (p.y > h) p.y = 0;
        ctx.beginPath();
        ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2);
        ctx.fillStyle = `rgba(57,160,218,${p.a})`;
        ctx.fill();
      });
      for (let i = 0; i < particles.length; i++) {
        for (let j = i + 1; j < particles.length; j++) {
          const dx = particles[i].x - particles[j].x;
          const dy = particles[i].y - particles[j].y;
          const d = Math.sqrt(dx * dx + dy * dy);
          if (d < 120) {
            ctx.beginPath();
            ctx.moveTo(particles[i].x, particles[i].y);
            ctx.lineTo(particles[j].x, particles[j].y);
            ctx.strokeStyle = `rgba(0,229,255,${0.12 * (1 - d / 120)})`;
            ctx.stroke();
          }
        }
      }
      raf = requestAnimationFrame(draw);
    };
    draw();
    const resize = () => { w = c.width = c.offsetWidth; h = c.height = c.offsetHeight; };
    window.addEventListener('resize', resize);
    return () => { cancelAnimationFrame(raf); window.removeEventListener('resize', resize); };
  }, []);
  return <canvas ref={canvasRef} className="absolute inset-0 w-full h-full pointer-events-none" />;
}

function SectionTitle({ tag, title, subtitle }) {
  return (
    <Reveal className="text-center mb-12 lg:mb-16">
      {tag && <span className="inline-block px-4 py-1.5 rounded-full bg-[#39A0DA]/10 border border-[#39A0DA]/30 text-[#00E5FF] text-xs font-heading tracking-[0.3em] uppercase mb-4">{tag}</span>}
      <h2 className="font-heading font-black text-3xl sm:text-4xl lg:text-5xl tracking-tight leading-tight text-white">{title}</h2>
      {subtitle && <p className="mt-4 max-w-2xl mx-auto font-body text-base lg:text-lg leading-relaxed text-white/60">{subtitle}</p>}
    </Reveal>
  );
}

function CourseCard({ tier, name, tagline, price, color, popular, features, delay }) {
  return (
    <Reveal delay={delay} className="group relative">
      {popular && (
        <div className="absolute -top-3 left-1/2 -translate-x-1/2 z-10 px-4 py-1 bg-gradient-to-r from-[#39A0DA] to-[#00E5FF] rounded-full text-[#050505] text-xs font-heading font-bold tracking-widest">MÁS POPULAR</div>
      )}
      <div
        className="relative h-full rounded-2xl p-[1px] overflow-hidden transition-all duration-500 group-hover:scale-[1.03]"
        style={{ background: `linear-gradient(135deg, ${color}40, transparent 50%, ${color}20)` }}
      >
        <div className="relative h-full rounded-2xl bg-[#0F1425] p-6 lg:p-8 flex flex-col overflow-hidden">
          <div className="absolute -top-20 -right-20 w-40 h-40 rounded-full opacity-0 group-hover:opacity-30 transition-opacity duration-700" style={{ background: `radial-gradient(circle, ${color}, transparent)` }} />
          <div className="relative z-10 flex flex-col h-full">
            <span className="font-heading font-black text-5xl lg:text-6xl bg-clip-text text-transparent mb-2" style={{ backgroundImage: `linear-gradient(135deg, ${color}, ${color}88)` }}>{tier}</span>
            <h3 className="font-heading font-bold text-xl text-white mb-1">{name}</h3>
            <p className="text-white/50 font-body text-sm mb-6">{tagline}</p>
            <div className="mb-6">
              <span className="font-heading font-black text-3xl text-white">{price}</span>
            </div>
            <ul className="space-y-3 flex-1 mb-8">
              {features.map((f, i) => (
                <li key={i} className="flex items-start gap-3 text-white/70 font-body text-sm">
                  <Check className="w-4 h-4 mt-0.5 flex-shrink-0" style={{ color }} />
                  {f}
                </li>
              ))}
            </ul>
            <a
              href="#cta"
              className="block text-center py-3.5 rounded-lg font-heading font-bold text-sm tracking-widest transition-all duration-300 hover:shadow-lg border"
              style={{
                background: popular ? `linear-gradient(135deg, ${color}, ${color}CC)` : 'transparent',
                color: popular ? '#050505' : color,
                borderColor: popular ? 'transparent' : `${color}40`,
              }}
            >
              ELEGIR PLAN
            </a>
          </div>
        </div>
      </div>
    </Reveal>
  );
}

function FAQItem({ q, a }) {
  const [open, setOpen] = useState(false);
  return (
    <div className="border-b border-white/10">
      <button onClick={() => setOpen(!open)} className="w-full flex items-center justify-between py-5 lg:py-6 text-left group">
        <span className="font-body font-semibold text-white text-sm lg:text-base pr-4 group-hover:text-[#00E5FF] transition-colors">{q}</span>
        <span className={`flex-shrink-0 w-8 h-8 rounded-full border border-[#39A0DA]/30 flex items-center justify-center transition-all duration-300 ${open ? 'bg-[#39A0DA] border-[#39A0DA] rotate-180' : ''}`}>
          {open ? <Minus className="w-4 h-4 text-[#050505]" /> : <Plus className="w-4 h-4 text-[#39A0DA]" />}
        </span>
      </button>
      <div className={`overflow-hidden transition-all duration-500 ${open ? 'max-h-60 pb-5' : 'max-h-0'}`}>
        <p className="text-white/60 font-body text-sm lg:text-base leading-relaxed">{a}</p>
      </div>
    </div>
  );
}

function TestimonialSlider() {
  const [idx, setIdx] = useState(0);
  useEffect(() => {
    const id = setInterval(() => setIdx(i => (i + 1) % TESTIMONIALS.length), 5000);
    return () => clearInterval(id);
  }, []);
  const t = TESTIMONIALS[idx];
  return (
    <div className="relative max-w-3xl mx-auto text-center">
      <Quote className="w-12 h-12 text-[#39A0DA]/30 mx-auto mb-6" />
      <p className="font-body text-lg lg:text-xl text-white/80 leading-relaxed italic mb-8 min-h-[120px] transition-all duration-500">"{t.quote}"</p>
      <div className="flex items-center justify-center gap-3 mb-8">
        <span className="text-3xl">{t.avatar}</span>
        <div className="text-left">
          <p className="font-heading font-bold text-white text-sm">{t.name}</p>
          <p className="font-body text-[#39A0DA] text-xs">{t.title}</p>
        </div>
      </div>
      <div className="flex justify-center gap-2">
        {TESTIMONIALS.map((_, i) => (
          <button key={i} onClick={() => setIdx(i)} className={`h-2 rounded-full transition-all duration-300 ${i === idx ? 'bg-[#39A0DA] w-8' : 'bg-white/20 hover:bg-white/40 w-2'}`} />
        ))}
      </div>
    </div>
  );
}

function Speedometer({ className = '' }) {
  const [ref, visible] = useInView(0.3);
  const [speed, setSpeed] = useState(0);
  useEffect(() => {
    if (!visible) return;
    let v = 0;
    const id = setInterval(() => {
      v += 3;
      if (v >= 320) { setSpeed(320); clearInterval(id); }
      else setSpeed(v);
    }, 16);
    return () => clearInterval(id);
  }, [visible]);
  const pct = speed / 360;
  const circumference = 2 * Math.PI * 45;
  return (
    <div ref={ref} className={`relative ${className}`}>
      <svg viewBox="0 0 100 100" className="w-full h-full">
        <circle cx="50" cy="50" r="45" fill="none" stroke="rgba(57,160,218,0.06)" strokeWidth="4" />
        <circle
          cx="50" cy="50" r="45" fill="none" stroke="url(#gaugeGrad)" strokeWidth="4"
          strokeDasharray={circumference}
          strokeDashoffset={circumference * (1 - pct)}
          strokeLinecap="round"
          className="transition-all duration-100"
          style={{ transform: 'rotate(-90deg)', transformOrigin: '50% 50%' }}
        />
        <defs>
          <linearGradient id="gaugeGrad" x1="0%" y1="0%" x2="100%" y2="0%">
            <stop offset="0%" stopColor="#39A0DA" />
            <stop offset="100%" stopColor="#00E5FF" />
          </linearGradient>
        </defs>
        <text x="50" y="48" textAnchor="middle" className="fill-white font-heading font-black" style={{ fontSize: '16px' }}>{speed}</text>
        <text x="50" y="60" textAnchor="middle" className="fill-white/40 font-body" style={{ fontSize: '6px' }}>KM/H</text>
      </svg>
    </div>
  );
}

/* ═══════════════════ PAGE ═══════════════════ */
export default function Home() {
  const [progress, setProgress] = useState(0);
  useEffect(() => {
    const h = () => {
      const d = document.documentElement;
      setProgress(d.scrollTop / (d.scrollHeight - d.clientHeight) * 100);
    };
    window.addEventListener('scroll', h, { passive: true });
    return () => window.removeEventListener('scroll', h);
  }, []);

  return (
    <div className="bg-[#050505] text-white font-body overflow-x-hidden selection:bg-[#39A0DA]/30 selection:text-white">
      {/* progress bar */}
      <div className="fixed top-0 left-0 right-0 h-[3px] z-[60]">
        <div className="h-full bg-gradient-to-r from-[#39A0DA] to-[#00E5FF] transition-all duration-150" style={{ width: `${progress}%` }} />
      </div>

      <Navbar />

      {/* ═══ 1. HERO ═══ */}
      <section id="hero" className="relative min-h-screen flex items-center justify-center overflow-hidden">
        <div className="absolute inset-0">
          <img src={HERO_BG} alt="Sim racing cockpit" className="w-full h-full object-cover" />
          <div className="absolute inset-0 bg-gradient-to-b from-[#050505]/80 via-[#050505]/60 to-[#050505]" />
        </div>
        <ParticleField />
        <div className="absolute top-0 left-0 w-full h-[1px] bg-gradient-to-r from-transparent via-[#39A0DA] to-transparent opacity-60" />
        <div className="absolute bottom-0 left-0 w-full h-[1px] bg-gradient-to-r from-transparent via-[#00E5FF] to-transparent opacity-40" />

        <div className="relative z-10 text-center px-4 max-w-5xl mx-auto">
          <Reveal delay={0}>
            <span className="inline-block px-5 py-2 rounded-full border border-[#39A0DA]/40 bg-[#39A0DA]/10 text-[#00E5FF] font-heading text-xs tracking-[0.3em] mb-6 lg:mb-8 backdrop-blur-sm">ACADEMIA PROFESIONAL DE SIM RACING</span>
          </Reveal>
          <Reveal delay={200}>
            <h1 className="font-heading font-black text-5xl sm:text-6xl md:text-7xl lg:text-8xl xl:text-9xl leading-[0.9] tracking-tight mb-4 lg:mb-6">
              NO <span className="bg-gradient-to-r from-[#39A0DA] to-[#00E5FF] bg-clip-text text-transparent">MANEJES</span>.<br />COMPITE.
            </h1>
          </Reveal>
          <Reveal delay={400}>
            <p className="font-body text-white/70 text-base sm:text-lg lg:text-xl max-w-2xl mx-auto mb-8 lg:mb-10 leading-relaxed">
              Convertite en piloto profesional de simulación y competí por premios reales.
            </p>
          </Reveal>
          <Reveal delay={600}>
            <div className="flex flex-col sm:flex-row gap-4 justify-center">
              <a href="#courses" className="group inline-flex items-center justify-center gap-2 px-8 py-4 bg-gradient-to-r from-[#39A0DA] to-[#00E5FF] text-[#050505] font-heading font-bold text-sm tracking-widest rounded-lg hover:shadow-2xl hover:shadow-[#39A0DA]/30 transition-all duration-300 hover:scale-105">
                COMENZAR MI CARRERA
                <ArrowRight className="w-4 h-4 group-hover:translate-x-1 transition-transform" />
              </a>
              <a href="#about" className="inline-flex items-center justify-center gap-2 px-8 py-4 border border-white/20 text-white font-heading font-bold text-sm tracking-widest rounded-lg hover:border-[#39A0DA]/50 hover:bg-white/5 transition-all duration-300">
                CONOCER MÁS
              </a>
            </div>
          </Reveal>
          <Reveal delay={800}>
            <p className="mt-6 text-white/40 text-xs font-body">⚡ Cupos limitados — Temporada 5 abre en 14 días</p>
          </Reveal>
        </div>

        {/* scroll indicator */}
        <div className="absolute bottom-8 left-1/2 -translate-x-1/2 flex flex-col items-center gap-2 animate-bounce z-10">
          <span className="text-white/50 text-xs font-body tracking-widest uppercase">Scroll</span>
          <ChevronDown className="w-5 h-5 text-[#39A0DA]" />
        </div>
      </section>

      {/* ═══ 2. STATS BAR ═══ */}
      <section className="relative py-12 lg:py-16 bg-[#0F1425] border-y border-[#39A0DA]/10">
        <div className="absolute inset-0 bg-gradient-to-r from-[#39A0DA]/5 via-transparent to-[#00E5FF]/5" />
        <div className="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 grid grid-cols-2 lg:grid-cols-4 gap-8">
          {STATS.map((s, i) => (
            <Reveal key={i} delay={i * 150} className="text-center">
              <s.icon className="w-6 h-6 text-[#39A0DA] mx-auto mb-3" />
              <div className="font-heading font-black text-3xl lg:text-4xl text-white mb-1">
                <CountUp end={s.value} prefix={s.prefix} suffix={s.suffix} />
              </div>
              <p className="font-body text-white/50 text-sm">{s.label}</p>
            </Reveal>
          ))}
        </div>
      </section>

      {/* ═══ 3. ABOUT ═══ */}
      <section id="about" className="py-20 lg:py-28">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <SectionTitle tag="La academia" title="¿Qué es APEX SIM-RACING?" subtitle="La primera academia profesional de simulación de carreras en Latinoamérica. Formamos pilotos competitivos con tecnología de punta y coaching de élite." />
          <div className="grid lg:grid-cols-2 gap-12 items-center">
            <Reveal direction="left">
              <div className="relative rounded-2xl overflow-hidden group">
                <img src={FACILITY} alt="APEX facility" className="w-full h-[350px] lg:h-[420px] object-cover group-hover:scale-105 transition-transform duration-700" />
                <div className="absolute inset-0 bg-gradient-to-t from-[#050505]/80 to-transparent" />
                <div className="absolute bottom-6 left-6">
                  <Speedometer className="w-20 h-20" />
                </div>
              </div>
            </Reveal>
            <div className="space-y-6">
              {[
                { icon: Target, title: 'Misión Competitiva', desc: 'Transformamos gamers casuales en pilotos profesionales de sim racing, capaces de competir en campeonatos internacionales.' },
                { icon: Shield, title: 'Metodología Probada', desc: 'Nuestro programa progresivo F3→F2→F1 está diseñado para desarrollar habilidades reales de conducción paso a paso.' },
                { icon: Star, title: 'Resultados Reales', desc: '+50 campeones formados, +$50.000 en premios entregados y una comunidad de +500 pilotos activos.' },
              ].map((item, i) => (
                <Reveal key={i} delay={i * 200} direction="right">
                  <div className="flex gap-4 p-5 rounded-xl bg-[#0F1425]/50 border border-white/5 hover:border-[#39A0DA]/20 transition-colors group">
                    <div className="w-12 h-12 rounded-lg bg-[#39A0DA]/10 flex items-center justify-center flex-shrink-0 group-hover:bg-[#39A0DA]/20 transition-colors">
                      <item.icon className="w-5 h-5 text-[#39A0DA]" />
                    </div>
                    <div>
                      <h3 className="font-heading font-bold text-white text-sm mb-1">{item.title}</h3>
                      <p className="font-body text-white/60 text-sm leading-relaxed">{item.desc}</p>
                    </div>
                  </div>
                </Reveal>
              ))}
            </div>
          </div>
        </div>
      </section>

      {/* ═══ 4. ROADMAP ═══ */}
      <section className="py-20 lg:py-28 bg-[#0F1425]/40">
        <div className="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
          <SectionTitle tag="Tu camino" title="Roadmap del Piloto" subtitle="Un programa progresivo que te lleva desde los fundamentos hasta la competencia profesional." />
          <div className="relative">
            <div className="absolute left-6 lg:left-1/2 top-0 bottom-0 w-[2px] bg-gradient-to-b from-[#39A0DA] via-[#00E5FF] to-[#39A0DA] opacity-30 -translate-x-1/2" />
            {ROADMAP.map((r, i) => (
              <Reveal key={i} delay={i * 250} direction={i % 2 === 0 ? 'left' : 'right'} className="relative mb-12 last:mb-0">
                <div className={`ml-16 lg:ml-0 ${i % 2 === 0 ? 'lg:w-[calc(50%-2rem)] lg:mr-auto' : 'lg:w-[calc(50%-2rem)] lg:ml-auto'}`}>
                  <div className="absolute left-6 lg:left-1/2 -translate-x-1/2 w-5 h-5 rounded-full bg-[#050505] border-2 border-[#39A0DA] shadow-lg shadow-[#39A0DA]/30 z-10 top-6" />
                  <div className="p-6 rounded-2xl bg-[#0F1425] border border-white/5 hover:border-[#39A0DA]/20 transition-all">
                    <span className="font-heading font-black text-4xl bg-gradient-to-r from-[#39A0DA] to-[#00E5FF] bg-clip-text text-transparent">{r.level}</span>
                    <h3 className="font-heading font-bold text-xl text-white mt-2">{r.title}</h3>
                    <span className="text-[#00E5FF] text-xs font-heading tracking-widest">{r.subtitle}</span>
                    <p className="font-body text-white/60 text-sm mt-3 leading-relaxed">{r.desc}</p>
                    <div className="flex flex-wrap gap-2 mt-4">
                      {r.benefits.map((b, j) => (
                        <span key={j} className="px-3 py-1 rounded-full bg-[#39A0DA]/10 text-[#39A0DA] text-xs font-body">{b}</span>
                      ))}
                    </div>
                  </div>
                </div>
              </Reveal>
            ))}
          </div>
        </div>
      </section>

      {/* ═══ 5. COURSES ═══ */}
      <section id="courses" className="py-20 lg:py-28">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <SectionTitle tag="Programas" title="Elegí tu Categoría" subtitle="Tres niveles diseñados para acompañarte desde tus primeros giros hasta el podio del campeonato." />
          <div className="grid md:grid-cols-3 gap-6 lg:gap-8">
            {COURSE_PLANS.map((c, i) => (
              <CourseCard key={i} {...c} delay={i * 200} />
            ))}
          </div>
        </div>
      </section>

      {/* ═══ 6. SIMULATION EXPERIENCE ═══ */}
      <section className="py-20 lg:py-28 bg-[#0F1425]/40 overflow-hidden">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <SectionTitle tag="Inmersión total" title="Experiencia de Simulación" subtitle="Equipos de última generación que replican la experiencia de conducir un monoplaza real." />
          <div className="grid md:grid-cols-2 gap-6">
            <Reveal direction="left" className="relative rounded-2xl overflow-hidden group h-[300px] lg:h-[400px]">
              <img src={SIM_EXP} alt="Cockpit simulation" className="w-full h-full object-cover group-hover:scale-110 transition-transform duration-700" />
              <div className="absolute inset-0 bg-gradient-to-t from-[#050505] via-transparent to-transparent" />
              <div className="absolute bottom-6 left-6">
                <span className="px-3 py-1 rounded-full bg-[#39A0DA]/20 text-[#00E5FF] text-xs font-heading tracking-widest backdrop-blur-sm">COCKPIT PRO</span>
                <h3 className="font-heading font-bold text-xl text-white mt-2">Volantes Direct Drive</h3>
              </div>
            </Reveal>
            <div className="grid grid-rows-2 gap-6">
              <Reveal delay={200} direction="right" className="relative rounded-2xl overflow-hidden group">
                <img src={TELEMETRY} alt="Telemetry screens" className="w-full h-full object-cover group-hover:scale-110 transition-transform duration-700" />
                <div className="absolute inset-0 bg-gradient-to-t from-[#050505] via-transparent to-transparent" />
                <div className="absolute bottom-4 left-4">
                  <span className="px-3 py-1 rounded-full bg-[#39A0DA]/20 text-[#00E5FF] text-xs font-heading tracking-widest backdrop-blur-sm">TELEMETRÍA</span>
                </div>
              </Reveal>
              <Reveal delay={400} direction="right" className="relative rounded-2xl overflow-hidden group">
                <img src={COURSES_IMG} alt="Multi-screen setup" className="w-full h-full object-cover group-hover:scale-110 transition-transform duration-700" />
                <div className="absolute inset-0 bg-gradient-to-t from-[#050505] via-transparent to-transparent" />
                <div className="absolute bottom-4 left-4">
                  <span className="px-3 py-1 rounded-full bg-[#39A0DA]/20 text-[#00E5FF] text-xs font-heading tracking-widest backdrop-blur-sm">TRIPLE SCREEN</span>
                </div>
              </Reveal>
            </div>
          </div>
        </div>
      </section>

      {/* ═══ 7. CHAMPIONSHIP ═══ */}
      <section id="championship" className="py-20 lg:py-28">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <SectionTitle tag="Competición" title="Campeonato Oficial" subtitle="Temporadas completas con clasificaciones semanales, rankings en tiempo real y finales con podio." />
          <Reveal>
            <div className="rounded-2xl bg-[#0F1425] border border-white/5 overflow-hidden">
              <div className="p-4 lg:p-6 bg-gradient-to-r from-[#39A0DA]/10 to-transparent border-b border-white/5">
                <div className="flex items-center gap-3">
                  <Trophy className="w-5 h-5 text-[#39A0DA]" />
                  <h3 className="font-heading font-bold text-white text-sm">STANDINGS — TEMPORADA 4</h3>
                  <span className="ml-auto px-3 py-1 rounded-full bg-green-500/10 text-green-400 text-xs font-body">EN VIVO</span>
                </div>
              </div>
              <div className="overflow-x-auto">
                <table className="w-full min-w-[500px]">
                  <thead>
                    <tr className="text-white/40 text-xs font-heading tracking-widest border-b border-white/5">
                      <th className="px-4 lg:px-6 py-3 text-left">POS</th>
                      <th className="px-4 lg:px-6 py-3 text-left">PILOTO</th>
                      <th className="px-4 lg:px-6 py-3 text-left hidden sm:table-cell">EQUIPO</th>
                      <th className="px-4 lg:px-6 py-3 text-right">PTS</th>
                    </tr>
                  </thead>
                  <tbody>
                    {STANDINGS.map((s, i) => (
                      <tr key={i} className={`border-b border-white/5 hover:bg-white/[0.02] transition-colors ${i === 0 ? 'bg-[#39A0DA]/5' : ''}`}>
                        <td className="px-4 lg:px-6 py-4">
                          <span className={`font-heading font-black text-lg ${i === 0 ? 'text-yellow-400' : i === 1 ? 'text-gray-300' : i === 2 ? 'text-amber-600' : 'text-white/40'}`}>{s.pos}</span>
                        </td>
                        <td className="px-4 lg:px-6 py-4">
                          <span className="text-white font-body font-semibold text-sm">{s.flag} {s.name}</span>
                        </td>
                        <td className="px-4 lg:px-6 py-4 hidden sm:table-cell text-white/50 font-body text-sm">{s.team}</td>
                        <td className="px-4 lg:px-6 py-4 text-right font-heading font-bold text-[#00E5FF]">{s.pts}</td>
                      </tr>
                    ))}
                  </tbody>
                </table>
              </div>
            </div>
          </Reveal>
        </div>
      </section>

      {/* ═══ 8. PRIZES & PODIUM ═══ */}
      <section id="prizes" className="py-20 lg:py-28 relative overflow-hidden">
        <div className="absolute inset-0">
          <img src={PODIUM_IMG} alt="Podium ceremony" className="w-full h-full object-cover opacity-20" />
          <div className="absolute inset-0 bg-gradient-to-b from-[#050505] via-[#050505]/90 to-[#050505]" />
        </div>
        <div className="relative max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
          <SectionTitle tag="Gloria" title="Premios y Podios" subtitle="Los campeones de APEX reciben reconocimiento, trofeos y premios en efectivo." />
          <div className="grid md:grid-cols-3 gap-6 lg:gap-8 items-end">
            {[
              { pos: '2do', emoji: '🥈', prize: '$3.000', color: '#C0C0C0', delay: 200, order: 'md:order-1 order-2', height: 'h-auto' },
              { pos: '1ero', emoji: '🥇', prize: '$5.000', color: '#FFD700', delay: 0, order: 'md:order-2 order-1', height: 'h-auto md:pb-14' },
              { pos: '3ero', emoji: '🥉', prize: '$1.500', color: '#CD7F32', delay: 400, order: 'md:order-3 order-3', height: 'h-auto' },
            ].map((p, i) => (
              <Reveal key={i} delay={p.delay} className={p.order}>
                <div className={`relative text-center p-8 lg:p-10 rounded-2xl bg-[#0F1425]/80 backdrop-blur-sm border border-white/5 hover:border-white/10 transition-all group ${p.height}`}>
                  <div className="absolute inset-0 rounded-2xl opacity-0 group-hover:opacity-100 transition-opacity duration-500" style={{ background: `radial-gradient(ellipse at center, ${p.color}10, transparent)` }} />
                  <div className="relative z-10">
                    <span className="text-5xl lg:text-6xl block mb-4">{p.emoji}</span>
                    <span className="font-heading font-bold text-lg" style={{ color: p.color }}>{p.pos} Puesto</span>
                    <div className="my-4 font-heading font-black text-4xl lg:text-5xl text-white">{p.prize}</div>
                    <p className="text-white/50 text-sm font-body">+ Trofeo oficial + Reconocimiento global</p>
                  </div>
                </div>
              </Reveal>
            ))}
          </div>
        </div>
      </section>

      {/* ═══ 9. TESTIMONIALS ═══ */}
      <section className="py-20 lg:py-28 bg-[#0F1425]/40">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <SectionTitle tag="Pilotos" title="Lo que dicen nuestros alumnos" subtitle="Historias reales de pilotos que transformaron su pasión en competencia." />
          <Reveal>
            <TestimonialSlider />
          </Reveal>
        </div>
      </section>

      {/* ═══ 10. WHY CHOOSE US ═══ */}
      <section className="py-20 lg:py-28">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <SectionTitle tag="Diferencial" title="¿Por qué elegirnos?" subtitle="Lo que nos hace la academia de sim racing número uno en la región." />
          <div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-6">
            {WHY_US.map((w, i) => (
              <Reveal key={i} delay={i * 150}>
                <div className="p-6 rounded-2xl bg-[#0F1425]/50 border border-white/5 hover:border-[#39A0DA]/20 transition-all group h-full">
                  <div className="w-12 h-12 rounded-lg bg-[#39A0DA]/10 flex items-center justify-center mb-4 group-hover:bg-[#39A0DA]/20 group-hover:shadow-lg group-hover:shadow-[#39A0DA]/10 transition-all">
                    <w.icon className="w-5 h-5 text-[#39A0DA]" />
                  </div>
                  <h3 className="font-heading font-bold text-white text-sm mb-2">{w.title}</h3>
                  <p className="font-body text-white/60 text-sm leading-relaxed">{w.desc}</p>
                </div>
              </Reveal>
            ))}
          </div>
        </div>
      </section>

      {/* ═══ 11. FAQ ═══ */}
      <section id="faq" className="py-20 lg:py-28 bg-[#0F1425]/40">
        <div className="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
          <SectionTitle tag="Dudas" title="Preguntas Frecuentes" />
          <Reveal>
            <div>
              {FAQS.map((f, i) => <FAQItem key={i} {...f} />)}
            </div>
          </Reveal>
        </div>
      </section>

      {/* ═══ 12. CTA FINAL ═══ */}
      <section id="cta" className="relative py-28 lg:py-40 overflow-hidden">
        <div className="absolute inset-0 bg-[#0F1425]">
          <div className="absolute inset-0" style={{
            backgroundImage: 'repeating-linear-gradient(90deg, transparent, transparent 60px, rgba(57,160,218,0.03) 60px, rgba(57,160,218,0.03) 61px)',
          }} />
          <div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[600px] h-[600px] rounded-full bg-[#39A0DA]/10 blur-[120px] animate-pulse" />
        </div>
        <div className="relative z-10 max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
          <Reveal>
            <span className="inline-block px-5 py-2 rounded-full border border-[#00E5FF]/30 bg-[#00E5FF]/10 text-[#00E5FF] font-heading text-xs tracking-[0.3em] mb-6">TEMPORADA 5</span>
          </Reveal>
          <Reveal delay={200}>
            <h2 className="font-heading font-black text-4xl sm:text-5xl lg:text-7xl leading-[0.95] tracking-tight text-white mb-6">
              LA PRÓXIMA TEMPORADA<br />
              <span className="bg-gradient-to-r from-[#39A0DA] to-[#00E5FF] bg-clip-text text-transparent">COMIENZA AHORA.</span>
            </h2>
          </Reveal>
          <Reveal delay={400}>
            <p className="font-body text-white/60 text-lg lg:text-xl mb-10 max-w-xl mx-auto">Los campeones no esperan la bandera verde.</p>
          </Reveal>
          <Reveal delay={600}>
            <a href="#courses" className="group inline-flex items-center justify-center gap-3 px-10 py-5 bg-gradient-to-r from-[#39A0DA] to-[#00E5FF] text-[#050505] font-heading font-black text-base lg:text-lg tracking-widest rounded-xl hover:shadow-2xl hover:shadow-[#39A0DA]/40 transition-all duration-300 hover:scale-105">
              QUIERO COMPETIR
              <ArrowRight className="w-5 h-5 group-hover:translate-x-1 transition-transform" />
            </a>
          </Reveal>
          <Reveal delay={800}>
            <p className="mt-6 text-white/30 text-xs font-body">Solo quedan 23 cupos disponibles. Inscripción cierra el 15 de febrero.</p>
          </Reveal>
        </div>
      </section>

      {/* ═══ FOOTER ═══ */}
      <footer className="py-12 bg-[#050505] border-t border-white/5">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="grid md:grid-cols-4 gap-8 mb-10">
            <div className="md:col-span-2">
              <div className="flex items-center gap-2 mb-4">
                <div className="w-8 h-8 rounded bg-gradient-to-br from-[#39A0DA] to-[#00E5FF] flex items-center justify-center font-heading font-black text-[#050505] text-sm">A</div>
                <span className="font-heading font-bold text-white text-lg tracking-wider">APEX<span className="text-[#39A0DA]"> SIM-RACING</span></span>
              </div>
              <p className="font-body text-white/40 text-sm leading-relaxed max-w-md">La primera academia profesional de simulación de carreras en Latinoamérica. Formamos campeones desde 2020.</p>
            </div>
            <div>
              <h4 className="font-heading font-bold text-white text-xs tracking-widest mb-4">NAVEGACIÓN</h4>
              <ul className="space-y-2">
                {[
                  { label: 'Inicio', href: '#hero' },
                  { label: 'Academia', href: '#about' },
                  { label: 'Cursos', href: '#courses' },
                  { label: 'Campeonato', href: '#championship' },
                  { label: 'FAQ', href: '#faq' },
                ].map(l => (
                  <li key={l.label}><a href={l.href} className="text-white/40 hover:text-[#00E5FF] transition-colors font-body text-sm">{l.label}</a></li>
                ))}
              </ul>
            </div>
            <div>
              <h4 className="font-heading font-bold text-white text-xs tracking-widest mb-4">CONTACTO</h4>
              <ul className="space-y-2 text-white/40 font-body text-sm">
                <li><a href="mailto:info@apexsimracing.com" className="hover:text-[#00E5FF] transition-colors">info@apexsimracing.com</a></li>
                <li><a href="tel:+5491155550000" className="hover:text-[#00E5FF] transition-colors">+54 9 11 5555-0000</a></li>
                <li>Buenos Aires, Argentina</li>
              </ul>
            </div>
          </div>
          <div className="border-t border-white/5 pt-6 flex flex-col sm:flex-row justify-between items-center gap-4">
            <p className="text-white/30 font-body text-xs">© 2025 APEX SIM-RACING. Todos los derechos reservados.</p>
            <div className="flex gap-4">
              <a href="https://instagram.com" target="_blank" rel="noopener noreferrer" className="text-white/30 hover:text-[#39A0DA] transition-colors text-xs font-body">Instagram</a>
              <a href="https://twitter.com" target="_blank" rel="noopener noreferrer" className="text-white/30 hover:text-[#39A0DA] transition-colors text-xs font-body">Twitter</a>
              <a href="https://discord.com" target="_blank" rel="noopener noreferrer" className="text-white/30 hover:text-[#39A0DA] transition-colors text-xs font-body">Discord</a>
              <a href="https://youtube.com" target="_blank" rel="noopener noreferrer" className="text-white/30 hover:text-[#39A0DA] transition-colors text-xs font-body">YouTube</a>
            </div>
          </div>
        </div>
      </footer>
    </div>
  );
}