/* ══════════════════════════════════════════
   APEX SIM-RACING — style.css
   Paleta: #050505 | #0F1425 | #39A0DA | #00E5FF
   Fuentes: Orbitron (headings) | Inter (body)
══════════════════════════════════════════ */

/* ── RESET & BASE ── */
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

html { scroll-behavior: smooth; font-size: 16px; }

body {
  background: #050505;
  color: #ffffff;
  font-family: 'Inter', sans-serif;
  overflow-x: hidden;
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
}

/* Selection */
::selection { background: rgba(57,160,218,0.3); color: #fff; }

/* Custom scrollbar */
::-webkit-scrollbar { width: 6px; }
::-webkit-scrollbar-track { background: #050505; }
::-webkit-scrollbar-thumb { background: rgba(57,160,218,0.25); border-radius: 3px; }
::-webkit-scrollbar-thumb:hover { background: rgba(57,160,218,0.5); }

/* ── TYPOGRAPHY ── */
h1,h2,h3,h4 { font-family: 'Orbitron', sans-serif; font-weight: 900; line-height: 1; }
.font-heading { font-family: 'Orbitron', sans-serif; }

/* ── UTILITIES ── */
.container    { max-width: 1280px; margin: 0 auto; padding: 0 1.5rem; }
.container-sm { max-width: 900px;  margin: 0 auto; padding: 0 1.5rem; }
.text-center  { text-align: center; }
.text-right   { text-align: right; }
.text-muted   { color: rgba(255,255,255,0.4); }

.gradient-text {
  background: linear-gradient(135deg, #39A0DA, #00E5FF);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

/* ── BADGE / PILL ── */
.badge {
  display: inline-block;
  padding: 6px 16px;
  border-radius: 999px;
  background: rgba(57,160,218,0.1);
  border: 1px solid rgba(57,160,218,0.3);
  color: #00E5FF;
  font-family: 'Orbitron', sans-serif;
  font-size: 0.65rem;
  letter-spacing: 0.3em;
  text-transform: uppercase;
  margin-bottom: 1rem;
}

/* ── BUTTONS ── */
.btn-primary {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
  padding: 1rem 2rem;
  background: linear-gradient(135deg, #39A0DA, #00E5FF);
  color: #050505;
  font-family: 'Orbitron', sans-serif;
  font-weight: 700;
  font-size: 0.75rem;
  letter-spacing: 0.15em;
  border-radius: 0.75rem;
  text-decoration: none;
  border: none;
  cursor: pointer;
  transition: transform 0.3s, box-shadow 0.3s;
}
.btn-primary:hover {
  transform: scale(1.05);
  box-shadow: 0 0 40px rgba(57,160,218,0.4);
}
.btn-primary.btn-xl {
  padding: 1.25rem 2.5rem;
  font-size: 0.9rem;
  border-radius: 1rem;
}

.btn-outline {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
  padding: 1rem 2rem;
  border: 1px solid rgba(255,255,255,0.2);
  color: #fff;
  font-family: 'Orbitron', sans-serif;
  font-weight: 700;
  font-size: 0.75rem;
  letter-spacing: 0.15em;
  border-radius: 0.75rem;
  text-decoration: none;
  background: transparent;
  cursor: pointer;
  transition: border-color 0.3s, background 0.3s;
}
.btn-outline:hover {
  border-color: rgba(57,160,218,0.5);
  background: rgba(255,255,255,0.05);
}

/* ── SECTION COMMON ── */
.section { padding: 5rem 0; }
@media(min-width:1024px){ .section { padding: 7rem 0; } }

.bg-dark { background: rgba(15,20,37,0.4); }

.section-title { text-align: center; margin-bottom: 3rem; }
.section-title h2 { font-size: clamp(1.8rem,4vw,3.2rem); margin-bottom: 1rem; }
.section-title p  { max-width: 600px; margin: 0 auto; color: rgba(255,255,255,0.6); font-size: 1rem; line-height: 1.7; }

/* ── REVEAL ANIMATIONS ── */
.reveal {
  opacity: 0;
  transform: translateY(48px);
  transition: opacity 0.7s ease-out, transform 0.7s ease-out;
}
.reveal.from-left  { transform: translateX(48px); }
.reveal.from-right { transform: translateX(-48px); }
.reveal.visible    { opacity: 1; transform: translate(0,0); }

/* ════════════════════════════════════════
   PROGRESS BAR
════════════════════════════════════════ */
#progress-bar {
  position: fixed;
  top: 0; left: 0; right: 0;
  height: 3px;
  z-index: 100;
  background: linear-gradient(90deg, #39A0DA, #00E5FF);
  width: 0%;
  transition: width 0.15s linear;
}

/* ════════════════════════════════════════
   NAVBAR
════════════════════════════════════════ */
#navbar {
  position: fixed;
  top: 0; left: 0; right: 0;
  z-index: 50;
  transition: background 0.5s, border-color 0.5s, box-shadow 0.5s;
}
#navbar.scrolled {
  background: rgba(5,5,5,0.95);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border-bottom: 1px solid rgba(57,160,218,0.2);
  box-shadow: 0 4px 30px rgba(57,160,218,0.05);
}
.nav-container {
  max-width: 1280px;
  margin: 0 auto;
  padding: 0 1.5rem;
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 72px;
}
.nav-logo {
  display: flex;
  align-items: center;
  gap: 8px;
  text-decoration: none;
  font-family: 'Orbitron', sans-serif;
  font-weight: 700;
  font-size: 1rem;
  color: #fff;
  letter-spacing: 0.1em;
  transition: none;
}
.logo-icon {
  width: 36px; height: 36px;
  border-radius: 6px;
  background: linear-gradient(135deg, #39A0DA, #00E5FF);
  display: flex; align-items: center; justify-content: center;
  color: #050505;
  font-family: 'Orbitron', sans-serif;
  font-weight: 900;
  font-size: 1rem;
  transition: box-shadow 0.3s;
}
.nav-logo:hover .logo-icon { box-shadow: 0 0 20px rgba(57,160,218,0.5); }
.logo-accent { color: #39A0DA; }

.nav-links {
  display: flex;
  align-items: center;
  gap: 2rem;
}
.nav-links a {
  color: rgba(255,255,255,0.7);
  text-decoration: none;
  font-size: 0.85rem;
  transition: color 0.3s;
  letter-spacing: 0.05em;
}
.nav-links a:hover { color: #00E5FF; }
.nav-links .nav-cta {
  padding: 0.5rem 1.25rem;
  background: linear-gradient(135deg,#39A0DA,#00E5FF);
  color: #050505;
  font-family: 'Orbitron', sans-serif;
  font-weight: 700;
  font-size: 0.65rem;
  letter-spacing: 0.15em;
  border-radius: 6px;
  transition: transform 0.3s, box-shadow 0.3s;
}
.nav-links .nav-cta:hover { transform: scale(1.05); box-shadow: 0 0 20px rgba(57,160,218,0.4); color:#050505; }

/* Hamburger */
.hamburger {
  display: none;
  flex-direction: column;
  gap: 5px;
  background: none;
  border: none;
  cursor: pointer;
  padding: 4px;
}
.hamburger span {
  display: block;
  width: 24px; height: 2px;
  background: #39A0DA;
  border-radius: 2px;
  transition: transform 0.3s, opacity 0.3s;
}
.hamburger.open span:nth-child(1) { transform: rotate(45deg) translate(5px,5px); }
.hamburger.open span:nth-child(2) { opacity: 0; }
.hamburger.open span:nth-child(3) { transform: rotate(-45deg) translate(5px,-5px); }

/* Mobile Menu */
.mobile-menu {
  display: none;
  flex-direction: column;
  background: rgba(5,5,5,0.98);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border-top: 1px solid rgba(57,160,218,0.2);
  padding: 1rem 1.5rem 1.5rem;
  gap: 1rem;
}
.mobile-menu.open { display: flex; }
.mobile-menu a {
  color: rgba(255,255,255,0.8);
  text-decoration: none;
  font-size: 0.95rem;
  transition: color 0.3s;
  padding: 4px 0;
}
.mobile-menu a:hover { color: #00E5FF; }
.mobile-menu .nav-cta {
  text-align: center;
  padding: 0.75rem;
  background: linear-gradient(135deg,#39A0DA,#00E5FF);
  color: #050505 !important;
  font-family: 'Orbitron', sans-serif;
  font-weight: 700;
  font-size: 0.65rem;
  letter-spacing: 0.15em;
  border-radius: 6px;
}

@media(max-width:1023px){ .nav-links { display:none; } .hamburger { display:flex; } }

/* ════════════════════════════════════════
   HERO
════════════════════════════════════════ */
.hero {
  position: relative;
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
}
.hero-bg {
  position: absolute; inset: 0;
}
.hero-bg img {
  width: 100%; height: 100%;
  object-fit: cover;
}
.hero-overlay {
  position: absolute; inset: 0;
  background: linear-gradient(to bottom, rgba(5,5,5,0.8), rgba(5,5,5,0.6) 50%, #050505);
}
#particles {
  position: absolute; inset: 0;
  width: 100%; height: 100%;
  pointer-events: none;
}
.hero-line {
  position: absolute;
  left: 0; right: 0;
  height: 1px;
}
.hero-line-top    { top: 0;    background: linear-gradient(90deg,transparent,rgba(57,160,218,0.6),transparent); }
.hero-line-bottom { bottom: 0; background: linear-gradient(90deg,transparent,rgba(0,229,255,0.4),transparent); }

.hero-content {
  position: relative;
  z-index: 10;
  text-align: center;
  padding: 0 1rem;
  max-width: 900px;
  margin: 0 auto;
}
.hero-title {
  font-size: clamp(3rem, 9vw, 8rem);
  letter-spacing: -0.02em;
  margin: 1rem 0;
  line-height: 0.9;
}
.hero-subtitle {
  color: rgba(255,255,255,0.7);
  font-size: clamp(1rem,2vw,1.2rem);
  max-width: 600px;
  margin: 0 auto 2.5rem;
  line-height: 1.7;
}
.hero-buttons {
  display: flex;
  flex-wrap: wrap;
  gap: 1rem;
  justify-content: center;
}
.hero-note {
  margin-top: 1.5rem;
  color: rgba(255,255,255,0.4);
  font-size: 0.75rem;
}

.scroll-indicator {
  position: absolute;
  bottom: 2rem;
  left: 50%;
  transform: translateX(-50%);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 6px;
  animation: bounce 2s infinite;
  z-index: 10;
}
.scroll-indicator span { color: rgba(255,255,255,0.5); font-size: 0.6rem; letter-spacing: 0.2em; text-transform: uppercase; }

@keyframes bounce {
  0%,100%{ transform:translateX(-50%) translateY(0); }
  50%    { transform:translateX(-50%) translateY(8px); }
}

/* ════════════════════════════════════════
   STATS BAR
════════════════════════════════════════ */
.stats-bar {
  position: relative;
  padding: 3.5rem 0;
  background: #0F1425;
  border-top: 1px solid rgba(57,160,218,0.1);
  border-bottom: 1px solid rgba(57,160,218,0.1);
}
.stats-bg {
  position: absolute; inset: 0;
  background: linear-gradient(90deg, rgba(57,160,218,0.05), transparent, rgba(0,229,255,0.05));
}
.stats-grid {
  display: grid;
  grid-template-columns: repeat(2,1fr);
  gap: 2rem;
}
@media(min-width:1024px){ .stats-grid{ grid-template-columns:repeat(4,1fr); } }

.stat-item {
  position: relative;
  text-align: center;
}
.stat-item svg { margin: 0 auto 0.75rem; display:block; }
.stat-number {
  font-family: 'Orbitron', sans-serif;
  font-weight: 900;
  font-size: clamp(1.8rem,3vw,2.5rem);
  color: #fff;
  margin-bottom: 0.25rem;
}
.stat-item p { color: rgba(255,255,255,0.5); font-size: 0.85rem; }

/* ════════════════════════════════════════
   ABOUT
════════════════════════════════════════ */
.about-grid {
  display: grid;
  gap: 3rem;
  align-items: center;
}
@media(min-width:1024px){ .about-grid{ grid-template-columns:1fr 1fr; } }

.about-image {
  position: relative;
  border-radius: 1rem;
  overflow: hidden;
}
.about-image img {
  width: 100%;
  height: 350px;
  object-fit: cover;
  display: block;
  transition: transform 0.7s;
}
.about-image:hover img { transform: scale(1.05); }
.about-image-overlay {
  position: absolute; inset: 0;
  background: linear-gradient(to top, rgba(5,5,5,0.8), transparent);
}
.speedometer-wrap {
  position: absolute;
  bottom: 1.5rem; left: 1.5rem;
  width: 80px; height: 80px;
}

.about-features { display: flex; flex-direction: column; gap: 1.25rem; }
.feature-card {
  display: flex;
  gap: 1rem;
  padding: 1.25rem;
  border-radius: 0.75rem;
  background: rgba(15,20,37,0.5);
  border: 1px solid rgba(255,255,255,0.05);
  transition: border-color 0.3s;
}
.feature-card:hover { border-color: rgba(57,160,218,0.2); }
.feature-icon {
  width: 48px; height: 48px;
  border-radius: 8px;
  background: rgba(57,160,218,0.1);
  display: flex; align-items: center; justify-content: center;
  flex-shrink: 0;
  transition: background 0.3s;
}
.feature-card:hover .feature-icon { background: rgba(57,160,218,0.2); }
.feature-card h3 { font-family:'Orbitron',sans-serif; font-size:0.85rem; color:#fff; margin-bottom:0.25rem; }
.feature-card p  { font-size:0.85rem; color:rgba(255,255,255,0.6); line-height:1.6; }

/* ════════════════════════════════════════
   ROADMAP
════════════════════════════════════════ */
.roadmap { position: relative; padding: 1rem 0; }

.roadmap-line {
  position: absolute;
  left: 24px;
  top: 0; bottom: 0;
  width: 2px;
  background: linear-gradient(to bottom, #39A0DA, #00E5FF, #39A0DA);
  opacity: 0.3;
}
@media(min-width:1024px){
  .roadmap-line { left: 50%; transform: translateX(-50%); }
}

.roadmap-item {
  position: relative;
  margin-bottom: 3rem;
  padding-left: 4rem;
}
@media(min-width:1024px){
  .roadmap-item {
    padding-left: 0;
    width: calc(50% - 2rem);
    margin-left: 0;
    margin-right: auto;
  }
  .roadmap-right {
    margin-left: auto;
    margin-right: 0;
  }
}

.roadmap-dot {
  position: absolute;
  left: 16px;
  top: 1.5rem;
  width: 20px; height: 20px;
  border-radius: 50%;
  background: #050505;
  border: 2px solid #39A0DA;
  box-shadow: 0 0 12px rgba(57,160,218,0.4);
  z-index: 10;
}
@media(min-width:1024px){
  .roadmap-dot {
    left: calc(50% - 10px + 2rem); /* right edge of left card */
  }
  .roadmap-right .roadmap-dot {
    left: calc(-2rem - 10px);
  }
}

.roadmap-card {
  padding: 1.5rem;
  border-radius: 1rem;
  background: #0F1425;
  border: 1px solid rgba(255,255,255,0.05);
  transition: border-color 0.3s;
}
.roadmap-card:hover { border-color: rgba(57,160,218,0.2); }
.roadmap-level {
  font-family: 'Orbitron', sans-serif;
  font-weight: 900;
  font-size: 2.5rem;
  background: linear-gradient(135deg,#39A0DA,#00E5FF);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}
.roadmap-card h3 { font-size:1.1rem; color:#fff; margin-top:0.5rem; }
.roadmap-sub { display:block; color:#00E5FF; font-family:'Orbitron',sans-serif; font-size:0.65rem; letter-spacing:0.2em; margin-top:2px; }
.roadmap-card p { color:rgba(255,255,255,0.6); font-size:0.85rem; margin-top:0.75rem; line-height:1.6; }
.roadmap-tags { display:flex; flex-wrap:wrap; gap:0.5rem; margin-top:1rem; }
.roadmap-tags span {
  padding: 4px 12px;
  border-radius: 999px;
  background: rgba(57,160,218,0.1);
  color: #39A0DA;
  font-size: 0.7rem;
}

/* ════════════════════════════════════════
   COURSES
════════════════════════════════════════ */
.courses-grid {
  display: grid;
  gap: 1.5rem;
}
@media(min-width:768px){ .courses-grid { grid-template-columns: repeat(3,1fr); } }

.course-card {
  position: relative;
  border-radius: 1rem;
  padding: 1px;
  overflow: hidden;
  background: linear-gradient(135deg, rgba(var(--cr),var(--cg),var(--cb),0.25), transparent 50%, rgba(var(--cr),var(--cg),var(--cb),0.12));
  transition: transform 0.5s;
}
.course-card:hover { transform: scale(1.03); }

/* Use color via CSS variable trick — inline style sets the color */
.course-card .course-glow {
  position: absolute;
  top: -5rem; right: -5rem;
  width: 10rem; height: 10rem;
  border-radius: 50%;
  opacity: 0;
  transition: opacity 0.7s;
  background: radial-gradient(circle, var(--card-color,#39A0DA), transparent);
}
.course-card:hover .course-glow { opacity: 0.3; }

.course-inner {
  position: relative;
  z-index: 1;
  border-radius: calc(1rem - 1px);
  background: #0F1425;
  padding: 2rem;
  display: flex;
  flex-direction: column;
  height: 100%;
}
.course-popular { border: 1px solid rgba(0,229,255,0.25) !important; }
.popular-badge {
  position: absolute;
  top: -14px;
  left: 50%;
  transform: translateX(-50%);
  z-index: 20;
  padding: 4px 16px;
  border-radius: 999px;
  background: linear-gradient(135deg,#39A0DA,#00E5FF);
  color: #050505;
  font-family: 'Orbitron', sans-serif;
  font-weight: 700;
  font-size: 0.6rem;
  letter-spacing: 0.15em;
  white-space: nowrap;
}

.course-tier {
  font-family: 'Orbitron', sans-serif;
  font-weight: 900;
  font-size: 3.5rem;
  background-image: linear-gradient(135deg,#39A0DA,#39A0DA88);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  display: block;
  margin-bottom: 0.25rem;
}
.course-inner h3 { font-size:1.1rem; color:#fff; }
.course-tagline  { color:rgba(255,255,255,0.5); font-size:0.8rem; margin-bottom:1.5rem; display:block; }
.course-price    { font-family:'Orbitron',sans-serif; font-weight:900; font-size:1.6rem; color:#fff; margin-bottom:1.5rem; }

.course-features {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
  flex: 1;
  margin-bottom: 2rem;
}
.course-features li {
  display: flex;
  align-items: flex-start;
  gap: 0.75rem;
  color: rgba(255,255,255,0.7);
  font-size: 0.85rem;
}
.course-features li svg { flex-shrink:0; margin-top:2px; }

.course-btn {
  display: block;
  text-align: center;
  padding: 0.875rem;
  border-radius: 0.5rem;
  font-family: 'Orbitron', sans-serif;
  font-weight: 700;
  font-size: 0.75rem;
  letter-spacing: 0.15em;
  text-decoration: none;
  transition: box-shadow 0.3s, opacity 0.3s;
  margin-top: auto;
}
.course-btn-outline {
  background: transparent;
  color: var(--btn-color,#39A0DA);
  border: 1px solid rgba(from var(--btn-color,#39A0DA) r g b / 0.4);
  border-color: color-mix(in srgb, var(--btn-color,#39A0DA) 40%, transparent);
}
.course-btn-outline:hover { opacity: 0.8; }
.course-btn-filled:hover { box-shadow: 0 0 30px rgba(0,229,255,0.3); }

/* ════════════════════════════════════════
   SIMULATION GRID
════════════════════════════════════════ */
.sim-grid {
  display: grid;
  gap: 1.5rem;
}
@media(min-width:768px){ .sim-grid{ grid-template-columns:1fr 1fr; } }

.sim-main,
.sim-card {
  position: relative;
  border-radius: 1rem;
  overflow: hidden;
}
.sim-main      { height: 300px; }
.sim-card      { height: 140px; }
.sim-stack     { display: grid; gap: 1.5rem; }

@media(min-width:1024px){
  .sim-main  { height: 400px; }
  .sim-card  { height: auto; }
}

.sim-main img,
.sim-card img {
  width: 100%; height: 100%;
  object-fit: cover;
  display: block;
  transition: transform 0.7s;
}
.sim-main:hover img,
.sim-card:hover img { transform: scale(1.1); }

.sim-overlay {
  position: absolute; inset: 0;
  background: linear-gradient(to top, #050505, transparent);
}
.sim-label {
  position: absolute;
  bottom: 1rem; left: 1rem;
}
.sim-label h3 { font-family:'Orbitron',sans-serif; font-size:1rem; color:#fff; margin-top:0.5rem; }

/* ════════════════════════════════════════
   CHAMPIONSHIP TABLE
════════════════════════════════════════ */
.standings-wrap {
  border-radius: 1rem;
  background: #0F1425;
  border: 1px solid rgba(255,255,255,0.05);
  overflow: hidden;
}
.standings-header {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  padding: 1.25rem 1.5rem;
  background: linear-gradient(90deg,rgba(57,160,218,0.1),transparent);
  border-bottom: 1px solid rgba(255,255,255,0.05);
  font-family: 'Orbitron', sans-serif;
  font-weight: 700;
  font-size: 0.75rem;
  color: #fff;
  letter-spacing: 0.1em;
}
.live-badge {
  margin-left: auto;
  padding: 4px 12px;
  border-radius: 999px;
  background: rgba(34,197,94,0.1);
  color: #4ade80;
  font-size: 0.7rem;
  font-family: 'Inter', sans-serif;
}

.standings-table {
  width: 100%;
  border-collapse: collapse;
  min-width: 500px;
}
.standings-table thead tr {
  color: rgba(255,255,255,0.4);
  font-family: 'Orbitron',sans-serif;
  font-size: 0.65rem;
  letter-spacing: 0.15em;
  border-bottom: 1px solid rgba(255,255,255,0.05);
}
.standings-table th { padding: 0.75rem 1.5rem; text-align: left; font-weight:600; }
.standings-table tbody tr {
  border-bottom: 1px solid rgba(255,255,255,0.05);
  transition: background 0.2s;
}
.standings-table tbody tr:hover { background: rgba(255,255,255,0.02); }
.row-gold { background: rgba(57,160,218,0.04); }
.standings-table td { padding: 1rem 1.5rem; font-size: 0.85rem; }

.pos { font-family:'Orbitron',sans-serif; font-weight:900; font-size:1.1rem; }
.pos-1 { color: #facc15; }
.pos-2 { color: #d1d5db; }
.pos-3 { color: #b45309; }
.pos-n { color: rgba(255,255,255,0.3); }
.pts   { font-family:'Orbitron',sans-serif; font-weight:700; color:#00E5FF; }

@media(max-width:640px){ .hide-sm{ display:none !important; } }

/* ════════════════════════════════════════
   PRIZES / PODIUM
════════════════════════════════════════ */
.prizes-section { position: relative; }
.prizes-bg {
  position: absolute; inset: 0;
}
.prizes-bg img { width:100%; height:100%; object-fit:cover; opacity:0.2; }
.prizes-overlay {
  position: absolute; inset: 0;
  background: linear-gradient(to bottom, #050505, rgba(5,5,5,0.9), #050505);
}
.podium-grid {
  display: grid;
  gap: 1.5rem;
  position: relative;
  z-index: 1;
}
@media(min-width:768px){
  .podium-grid {
    grid-template-columns: repeat(3,1fr);
    align-items: end;
  }
  .podium-first { padding-bottom: 3.5rem !important; }
}

.podium-card {
  position: relative;
  text-align: center;
  padding: 2.5rem 2rem;
  border-radius: 1rem;
  background: rgba(15,20,37,0.8);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
  border: 1px solid rgba(255,255,255,0.05);
  overflow: hidden;
  transition: border-color 0.3s;
}
.podium-card:hover { border-color: rgba(255,255,255,0.1); }
.podium-card::before {
  content:'';
  position: absolute; inset: 0;
  border-radius: inherit;
  background: radial-gradient(ellipse at center, color-mix(in srgb,var(--podium-color) 10%,transparent), transparent);
  opacity: 0;
  transition: opacity 0.5s;
}
.podium-card:hover::before { opacity: 1; }

.podium-emoji { display:block; font-size:3.5rem; margin-bottom:1rem; }
.podium-pos   { font-family:'Orbitron',sans-serif; font-weight:700; font-size:1rem; display:block; }
.podium-prize {
  font-family:'Orbitron',sans-serif; font-weight:900;
  font-size:clamp(2rem,4vw,2.8rem);
  color:#fff; margin:1rem 0;
}
.podium-card p { color:rgba(255,255,255,0.5); font-size:0.8rem; }

/* ════════════════════════════════════════
   TESTIMONIALS
════════════════════════════════════════ */
.testimonial-slider {
  max-width: 720px;
  margin: 0 auto;
  text-align: center;
}
.quote-icon { margin: 0 auto 1.5rem; display:block; }
.testimonial-text {
  font-size:clamp(1rem,2vw,1.2rem);
  color:rgba(255,255,255,0.8);
  font-style:italic;
  line-height:1.8;
  min-height:130px;
  margin-bottom:2rem;
  transition: opacity 0.4s;
}
.testimonial-text.fade { opacity:0; }
.testimonial-author {
  display:flex;
  align-items:center;
  justify-content:center;
  gap:0.75rem;
  margin-bottom:2rem;
}
.testimonial-avatar { font-size:2rem; }
.testimonial-name  { font-family:'Orbitron',sans-serif; font-weight:700; color:#fff; font-size:0.85rem; }
.testimonial-title { color:#39A0DA; font-size:0.75rem; }

.testimonial-dots { display:flex; justify-content:center; gap:0.5rem; }
.dot {
  height: 8px;
  border-radius: 999px;
  background: rgba(255,255,255,0.2);
  border: none;
  cursor: pointer;
  transition: background 0.3s, width 0.3s;
  width: 8px;
  padding: 0;
}
.dot:hover  { background: rgba(255,255,255,0.4); }
.dot-active { background: #39A0DA !important; width: 32px; }

/* ════════════════════════════════════════
   WHY US
════════════════════════════════════════ */
.why-grid {
  display: grid;
  gap: 1.5rem;
}
@media(min-width:640px)  { .why-grid{ grid-template-columns:repeat(2,1fr); } }
@media(min-width:1024px) { .why-grid{ grid-template-columns:repeat(3,1fr); } }

.why-card {
  padding: 1.5rem;
  border-radius: 1rem;
  background: rgba(15,20,37,0.5);
  border: 1px solid rgba(255,255,255,0.05);
  transition: border-color 0.3s;
}
.why-card:hover { border-color: rgba(57,160,218,0.2); }
.why-icon {
  width: 48px; height: 48px;
  border-radius: 8px;
  background: rgba(57,160,218,0.1);
  display: flex; align-items: center; justify-content: center;
  margin-bottom: 1rem;
  transition: background 0.3s, box-shadow 0.3s;
}
.why-card:hover .why-icon { background: rgba(57,160,218,0.2); box-shadow: 0 0 20px rgba(57,160,218,0.1); }
.why-card h3 { font-family:'Orbitron',sans-serif; font-size:0.8rem; color:#fff; margin-bottom:0.5rem; }
.why-card p  { font-size:0.85rem; color:rgba(255,255,255,0.6); line-height:1.6; }

/* ════════════════════════════════════════
   FAQ
════════════════════════════════════════ */
.faq-list { display: flex; flex-direction: column; }
.faq-item { border-bottom: 1px solid rgba(255,255,255,0.1); }

.faq-q {
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 1.25rem 0;
  background: none;
  border: none;
  color: #fff;
  font-family: 'Inter', sans-serif;
  font-weight: 600;
  font-size: 0.95rem;
  text-align: left;
  cursor: pointer;
  gap: 1rem;
  transition: color 0.3s;
}
.faq-q:hover { color: #00E5FF; }

.faq-icon {
  flex-shrink: 0;
  width: 32px; height: 32px;
  border-radius: 50%;
  border: 1px solid rgba(57,160,218,0.3);
  display: flex; align-items: center; justify-content: center;
  font-size: 1.2rem;
  color: #39A0DA;
  transition: background 0.3s, border-color 0.3s, transform 0.3s, color 0.3s;
  line-height: 1;
}
.faq-item.open .faq-icon {
  background: #39A0DA;
  border-color: #39A0DA;
  color: #050505;
  transform: rotate(45deg);
}

.faq-a {
  max-height: 0;
  overflow: hidden;
  transition: max-height 0.5s ease, padding 0.3s;
  color: rgba(255,255,255,0.6);
  font-size: 0.9rem;
  line-height: 1.7;
  padding: 0;
}
.faq-item.open .faq-a {
  max-height: 200px;
  padding-bottom: 1.25rem;
}

/* ════════════════════════════════════════
   CTA FINAL
════════════════════════════════════════ */
.cta-section {
  position: relative;
  background: #0F1425;
  padding: 7rem 0 9rem;
  overflow: hidden;
}
.cta-lines {
  position: absolute; inset: 0;
  background-image: repeating-linear-gradient(90deg, transparent, transparent 60px, rgba(57,160,218,0.03) 60px, rgba(57,160,218,0.03) 61px);
}
.cta-glow {
  position: absolute;
  top: 50%; left: 50%;
  transform: translate(-50%,-50%);
  width: 600px; height: 600px;
  border-radius: 50%;
  background: rgba(57,160,218,0.1);
  filter: blur(120px);
  animation: pulse 3s ease-in-out infinite;
}
@keyframes pulse { 0%,100%{opacity:1;} 50%{opacity:0.5;} }

.cta-title {
  font-size: clamp(2rem,6vw,5rem);
  line-height: 1;
  margin: 1rem 0 1.5rem;
}
.cta-sub  { font-size:1.1rem; color:rgba(255,255,255,0.6); margin-bottom:2.5rem; max-width:480px; margin-left:auto; margin-right:auto; }
.cta-note { margin-top:1.5rem; color:rgba(255,255,255,0.3); font-size:0.75rem; }

/* ════════════════════════════════════════
   FOOTER
════════════════════════════════════════ */
.footer {
  background: #050505;
  border-top: 1px solid rgba(255,255,255,0.05);
  padding: 3rem 0;
}
.footer-grid {
  display: grid;
  gap: 2rem;
  margin-bottom: 2.5rem;
}
@media(min-width:768px){ .footer-grid{ grid-template-columns:2fr 1fr 1fr; } }

.footer-logo {
  display: flex; align-items: center; gap: 8px;
  font-family: 'Orbitron',sans-serif; font-weight:700;
  font-size: 1rem; color:#fff; margin-bottom:1rem;
}
.footer-brand p { color:rgba(255,255,255,0.4); font-size:0.85rem; line-height:1.7; max-width:380px; }
.footer-nav h4, .footer-contact h4 {
  font-family:'Orbitron',sans-serif; font-size:0.65rem;
  letter-spacing:0.2em; color:#fff; margin-bottom:1rem;
}
.footer-nav ul, .footer-contact ul { list-style:none; display:flex; flex-direction:column; gap:0.5rem; }
.footer-nav a, .footer-contact a, .footer-contact li {
  color:rgba(255,255,255,0.4);
  text-decoration:none;
  font-size:0.85rem;
  transition:color 0.3s;
}
.footer-nav a:hover, .footer-contact a:hover { color:#00E5FF; }

.footer-bottom {
  border-top: 1px solid rgba(255,255,255,0.05);
  padding-top: 1.5rem;
  display: flex;
  flex-direction: column;
  gap: 1rem;
  align-items: center;
}
@media(min-width:640px){ .footer-bottom{ flex-direction:row; justify-content:space-between; } }
.footer-bottom p { color:rgba(255,255,255,0.3); font-size:0.75rem; }
.footer-social { display:flex; gap:1rem; }
.footer-social a { color:rgba(255,255,255,0.3); text-decoration:none; font-size:0.75rem; transition:color 0.3s; }
.footer-social a:hover { color:#39A0DA; }